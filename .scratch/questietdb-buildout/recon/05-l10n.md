# Recon 05 — Entity Localization Data (Questie `Localization/lookups/`)

All paths relative to `/home/logon/projects/Questie-clones/Questie-toc/Questie/` unless absolute.
Every number below was produced by actually `loadfile()`-ing the files under Lua 5.1
(`/usr/bin/lua5.1` is installed), not by eyeballing. Scan scripts live in
`/tmp/claude-1000/-home-logon-projects-Questie-clones-Questie-toc/9922907c-9560-4513-8eee-5d76191da8e4/scratchpad/scan{,2,3,4,5}.lua`.

---

## 1. Complete map of `Localization/lookups/`

### 1.1 Shape

```
Localization/lookups/
├── lookupOverrides.lua           13747 B   (locale-gated overrides, if/elseif on GetLocale())
├── lookupOverrides.test.lua      11708 B
├── lookupQuestCategories.lua      1842 B
├── lookupZones.lua               98926 B
├── lookupZonesCorrections.lua    54145 B
├── Classic/
│   ├── lookupLoadstrings.test.lua  21337 B
│   ├── lookupItems/   { 9 locale .lua + lookupItems.xml }
│   ├── lookupNpcs/    { 9 locale .lua + lookupNpcs.xml }
│   ├── lookupObjects/ { 9 locale .lua + lookupObjects.xml }
│   └── lookupQuests/  { 9 locale .lua + lookupQuests.xml }
├── TBC/    (same 4 subdirs, lookupLoadstrings.test.lua 21189 B)
├── Wotlk/  (same 4 subdirs, lookupLoadstrings.test.lua 21263 B)
├── Cata/   (same 4 subdirs, lookupLoadstrings.test.lua 21226 B)
└── MoP/    (same 4 subdirs, lookupLoadstrings.test.lua 21189 B)
```

**Expansion dir names (exact, case-sensitive): `Classic`, `TBC`, `Wotlk`, `Cata`, `MoP`.**
Note `Wotlk` (lowercase `otlk`), not `WotLK`/`WOTLK`.

**Lookup subdir names (exact): `lookupItems`, `lookupNpcs`, `lookupObjects`, `lookupQuests`.**

**5 expansions × 4 types × 9 locales = 180 locale data files. Plus 20 `.xml` manifests
(one per expansion/type) and 5 `lookupLoadstrings.test.lua`.**

### 1.2 Locales present — exactly 9, no enUS

```
deDE, esES, esMX, frFR, koKR, ptBR, ruRU, zhCN, zhTW
```

Confirmed two ways:
* every `lookup*/*.xml` lists exactly those 9 `<Script file="XX.lua"/>` entries, in that order;
* `l10n.lua:32-43` `supportedLocals` = those 9 **plus `enUS`** (enUS is the base/no-op locale).

There is **no `enUS.lua` anywhere** under `lookups/`. Base entity data in
`Database/<Exp>/<exp>{Quest,Npc,Object,Item}DB.lua` is already English. This matches
`QuestieDB/src/config.lua:79`:
```lua
config.locales = { "deDE", "esES", "esMX", "frFR", "koKR", "ptBR", "ruRU", "zhCN", "zhTW" }
```

### 1.3 Exact file sizes (bytes)

`lookupQuests/`
| locale | Classic | TBC | Wotlk | Cata | MoP |
|---|---|---|---|---|---|
| deDE |  590252 | 1175492 | 1307305 | 1784651 | 2076842 |
| esES |  591603 | 1040425 | 1299029 | 1742264 | 2066346 |
| esMX |  598452 |  949993 | 1299495 | 1720194 | 1719832 |
| frFR |  605796 | 1164126 | 1345294 | 1858276 | 2166948 |
| koKR |  693782 | 1194834 | 1453774 | 1991462 | 2352337 |
| ptBR |  582743 |  980203 | 1229474 | 1699697 | 2002126 |
| ruRU |  915768 | 1761299 | 1991597 | 2705700 | 3156319 |
| zhCN |  501532 |  789190 | 1137504 | 1501777 | 1737925 |
| zhTW |  500887 |  737644 | 1063320 | 1452031 | 1452099 |

`lookupNpcs/`
| locale | Classic | TBC | Wotlk | Cata | MoP |
|---|---|---|---|---|---|
| deDE | 376398 |  728135 | 1118138 | 1811249 | 2238915 |
| esES | 386128 |  741910 | 1146533 | 1858244 | 2294806 |
| esMX | 387094 |  386150 | 1146350 | 1858004 | 2294739 |
| frFR | 385817 |  750326 | 1154369 | 1866390 | 2304767 |
| koKR | 407661 |  774288 | 1184160 | 1900523 | 2341413 |
| ptBR | 381826 |  735139 | 1134454 | 1840870 | 2277622 |
| ruRU | 573906 | 1086731 | 1639957 | 2582650 | 3175141 |
| zhCN | 353038 |  682391 | 1052061 | 1714565 | 2107231 |
| zhTW | 658950 |  658959 | 1055411 | 1719103 | 2119933 |

`lookupObjects/`
| locale | Classic | TBC | Wotlk | Cata | MoP |
|---|---|---|---|---|---|
| deDE |  41744 |  75028 |  75028 | 597970 | 628999 |
| esES |  41821 |  76247 |  76247 | 629574 | 660783 |
| esMX | 177791 | 177093 | 177087 | 629574 | 658459 |
| frFR |  42910 |  76527 |  76527 | 640255 | 672141 |
| koKR |  40491 |  66924 |  66924 | 596516 | 626958 |
| ptBR |  41067 |  75241 |  75241 | 122717 | 153553 |
| ruRU |  62686 | 117080 | 117080 | 882636 | 927964 |
| zhCN |  33140 |  54307 | 181401 | 527200 | 552814 |
| zhTW | 316290 | 316032 | 316032 | 525927 | 549944 |

`lookupItems/`
| locale | Classic | TBC | Wotlk | Cata | MoP |
|---|---|---|---|---|---|
| deDE |  849125 |  983899 | 1416401 | 2674063 | 3778902 |
| esES |  872314 |  980918 | 1447256 | 2723446 | 3834775 |
| esMX |  872772 |   18884 | 1446785 | 2722882 | 3834263 |
| frFR |  875524 | 1036643 | 1461959 | 2767167 | 3907287 |
| koKR |  851390 | 1015412 | 1434128 | 2716095 | 3836084 |
| ptBR |  868389 |  986546 | 1435489 | 2704254 | 3811367 |
| ruRU | 1289341 | 1570402 | 2172010 | 3861346 | 5411813 |
| zhCN |  704311 |  801856 | 1172879 | 2274890 | 3194763 |
| zhTW |  701154 |  797794 | 1148405 | 2255015 | 3164230 |

XML manifest sizes: 335 B for Classic/TBC/Wotlk/Cata, 512 B for MoP (different xmlns header).

---

## 7. Total on-disk size per expansion

Data files only (36 locale `.lua` per expansion; excludes `.xml` and `lookupLoadstrings.test.lua`):

| Expansion | data files | bytes | MiB |
|---|---|---|---|
| Classic |  36 |  18 173 893 |  17.33 |
| TBC     |  36 |  25 564 068 |  24.38 |
| Wotlk   |  36 |  37 055 104 |  35.34 |
| Cata    |  36 |  63 459 177 |  60.52 |
| MoP     |  36 |  80 090 440 |  76.38 |
| **Total** | **180** | **224 342 682** | **213.95** |

Whole-tree (`du -sb`, includes xml + test + shared lookup*.lua at root):
Classic 18 217 050 · TBC 25 607 077 · Wotlk 37 098 187 · Cata 63 502 223 · MoP 80 134 157 ·
**tree total 224 743 158 B (214.3 MiB)**.

Per-type totals (`du -sb */*/`): e.g. `MoP/lookupItems/` = 34 778 092 B is the single biggest
directory; `Classic/lookupObjects/` = 802 371 B the smallest.

---

## 2. Exact file format (verified on Classic/deDE, uniform across all 180 files)

Every locale data file has this shape. **Header is 7 lines, payload begins line 8, EOF line is
`}]])`** (all 180 files end with exactly `}]])`).

### `Localization/lookups/Classic/lookupQuests/deDE.lua`
```lua
 1  if GetLocale() ~= "deDE" then
 2      return
 3  end
 4
 5  ---@type l10n
 6  local l10n = QuestieLoader:ImportModule("l10n")
 7
 8  l10n.questLookup["deDE"] = loadstring([[return {
 9  [2] = {"Klaue von Scharfkralle", {"Bringt die Klaue von Scharfkralle zu Senani Thunderheart im Splintertreeposten in Ashenvale."}},
10  [5] = {"Jitters' knurrender Magen", {"Sprecht mit Küchenchef Grual."}},
...
    }]])
```

### `Localization/lookups/Classic/lookupNpcs/deDE.lua`
```lua
 8  l10n.npcNameLookup["deDE"] = loadstring([[return {
 9  [3] = {"Fleischfresser",nil},
...
14  [54] = {"Corina Steele","Waffenschmiedin"},
```

### `Localization/lookups/Classic/lookupObjects/deDE.lua`
```lua
 8  l10n.objectLookup["deDE"] = loadstring([[return {
 9  [31] = "Alte Löwenstatue",
10  [32] = "Versunkene Truhe",
```

### `Localization/lookups/Classic/lookupItems/deDE.lua`
```lua
 8  l10n.itemLookup["deDE"] = loadstring([[return {
 9  [25] = "Abgenutztes Kurzschwert",
10  [35] = "Gebogener Stab",
```

### Format facts

| Question | Answer |
|---|---|
| Client-locale guard | `if GetLocale() ~= "<LOCALE>" then` / `    return` / `end` — **lines 1-3, byte-identical in all 180 files** (verified: 9 distinct line-1 strings × 20 files each) |
| Assignment target | `l10n.<field>["<LOCALE>"]` where field ∈ `questLookup`, `npcNameLookup`, `objectLookup`, `itemLookup` (verified exactly 1 assignment per file, 180/180) |
| Payload form | **`loadstring([[return { … }]])`** — the value assigned is a **function**, not a table. `l10n.questLookup["deDE"]` is a `function` that returns the table when called. This is uniform: all 180 files, all 4 types. |
| Long-bracket level | Always plain `[[ … ]]` (level 0). No `[=[` anywhere. |
| Import line (line 6) | `local l10n = QuestieLoader:ImportModule("l10n")` — identical in all 180 |
| Annotation (line 5) | **NOT uniform**: 144 files use `---@type l10n`, **36 files use `-- - @type l10n`** (broken annotation, cosmetic only). The 36 are all of `Cata/lookupItems/*`, `Cata/lookupNpcs/*`, `Wotlk/lookupItems/*`, `Wotlk/lookupNpcs/*` (9 locales × 4 dirs). |
| Extra header text | `MoP/lookupQuests/esMX.lua` and `MoP/lookupQuests/zhTW.lua` insert 3 extra lines before the assignment: blank / `-- ! THIS IS A RAW COPY OF THE CATA DATA, NOT AN ACTUAL MOP DUMP` / blank — so their assignment is on **line 12, not line 8**. Do not hardcode line 8. |

### Row shapes (positional field meanings)

```
lookupQuests:   [questId]  = { <name:string?>, <objectivesText: {string, ...}?> }
lookupNpcs:     [npcId]    = { <name:string?>, <subName:string?> }
lookupObjects:  [objectId] = <name:string>                 -- bare string, no table
lookupItems:    [itemId]   = <name:string>                 -- bare string, no table
```

Confirmed by loading all 180 files: **zero rows anywhere have a field index other than 1 or 2**
(scan reported `extra=-` for every file). This matches the shape assertions in
`Localization/lookups/<Exp>/lookupLoadstrings.test.lua`, whose comment at line ~72 says:

> ```lua
> -- Current quest lookups map numeric quest IDs to {title?, objectiveLines?}.
> -- The legacy format used {title?, descriptionLines?, objectiveLines?}; if that returns,
> -- l10n.lua and this schema validator must change together because field 3 is ignored today.
> ```

Objective lines (`row[2]`) are a **contiguous numeric-indexed array of strings** — verified
0 holes across all 45 quest files. Max array length observed = **15**. Distribution
(Classic/deDE, 4244 rows): `0 lines`=688, `1`=3317, `3`=186, `5`=39, `6`=2, `7`=4, `9`=6,
`11`=1, `15`=1. **Only odd counts + 0 and 6 occur** because paragraph breaks are stored as
`""` elements. Example `Classic/lookupQuests/deDE.lua:3007`:
```lua
[7636] = {"Holzstab der Uralten", {"Ihr müsst die vier dämonischen Verderber ausfindig machen und zerstören:","","Simone die Verführerin.","","Klinfran der Wahnsinnige.","", … }},
```
**27 878 empty-string (`""`) objective lines exist across the whole tree.** They are
semantically load-bearing (blank paragraphs) — an encoder that drops empty segments corrupts
the text.

Longest single string observed anywhere: **776 bytes** (`TBC/lookupQuests/ruRU.lua`, quest
8141, objective line 1). Total strings across the whole tree: **5 021 326**.

---

## 3. Which fields are translated — VERDICT: DESIGN.md is CORRECT

`QuestieDB/DESIGN.md:478-479` claims:
> Field coverage already matches exactly: quest `name` + `objectivesText`, npc `name` +
> `subName`, item `name`, object `name`.

**Verified true, no divergence.** Both the data shape (above) and the consumer
(`Localization/l10n.lua:59-96`) write exactly those four entity fields and nothing else:

| Entity | Translated fields | Key index (Classic; same in all flavors) |
|---|---|---|
| Quest  | `name`, `objectivesText` | `questKeys.name = 1`, `questKeys.objectivesText = 8` |
| Npc    | `name`, `subName`        | `npcKeys.name = 1`, `npcKeys.subName = 14` |
| Object | `name`                   | `objectKeys.name = 1` |
| Item   | `name`                   | `itemKeys.name = 1` |

Key indices read from `Database/Classic/classicQuestDB.lua:6`, `classicNpcDB.lua:6`,
`classicObjectDB.lua:6`, `classicItemDB.lua:6`.

Notes on nullability that the DESIGN sentence does not cover:
* Quest `name` may be **nil while the row still exists**: `Classic/lookupQuests/deDE.lua:614`
  is literally `[636] = {nil, nil},` — a completely empty row. Rows with nil name:
  Classic/deDE,esES,frFR,ptBR,ruRU = 1 each (id 636); Classic/koKR = 33 ids
  (`7421..7428,7886..7888,7921..7925,8081,8124,8157..8159,8163..8165,8267,8269,8289,8292,8293,8296,8298,8300,9017`);
  Classic/zhTW = 33 ids (different set, incl. `7361,7362,7363,7401,…,9001,9005,9008`).
* Npc rows may be `{nil,nil}`: `TBC/lookupNpcs/deDE.lua:10942` `[17165] = {nil,nil},` and
  `:11340` `[17582] = {nil,nil},` (present in all TBC npc files except esMX/zhTW which are
  stale copies). **No row anywhere has subName without name.**
* Npc `subName` is nil for the large majority: TBC/deDE has 17 390 rows → 3 784 with subName,
  13 604 name-only, 2 fully empty.
* No empty-string item/object names anywhere (0 hits).

---

## 4. Loading all nine locales in ONE generation run

### 4.1 The guard, quoted verbatim from four different locale files

`Localization/lookups/Classic/lookupQuests/koKR.lua:1-3`
```lua
if GetLocale() ~= "koKR" then
    return
end
```
`Localization/lookups/Wotlk/lookupObjects/ruRU.lua:1-3`
```lua
if GetLocale() ~= "ruRU" then
    return
end
```
`Localization/lookups/MoP/lookupItems/zhCN.lua:1-3`
```lua
if GetLocale() ~= "zhCN" then
    return
end
```
`Localization/lookups/Cata/lookupNpcs/ptBR.lua:1-3`
```lua
if GetLocale() ~= "ptBR" then
    return
end
```

**Uniformity proof**: `head -1` over all 180 files yields exactly 9 distinct strings,
20 occurrences each (one per expansion×type). The guard is a *top-level early return from the
chunk*, so once `GetLocale()` matches, the rest of the file executes.

### 4.2 What must be stubbed

Only **two globals**:

1. `_G.GetLocale` — a closure over a mutable upvalue you flip before each `loadfile`.
2. `_G.QuestieLoader` — needs only `ImportModule(name)` returning the l10n table. The files
   never inspect the argument.

Nothing else is touched: no `Questie`, no `QuestieDB`, no WoW API. `loadstring` is standard
Lua 5.1 and works as-is (in Lua 5.2+ you must alias `loadstring = load`).

### 4.3 What must be RESET between files

The assignment is `l10n.<field>["<locale>"] = <function>`, keyed by locale — so writes for
different locales do **not** collide, and in principle one shared `l10n` table could
accumulate all nine. **But reset anyway**, because:

* If `GetLocale()` and the file's locale ever disagree the file silently no-ops, and with a
  shared table you would read a *stale sibling locale's* function and not notice. With a fresh
  table per file, `type(l10n[field][locale]) ~= "function"` is a hard error.
* The returned closure is memory-heavy; calling it materialises a table of up to 88 183 rows.
  Drop the reference after each file so GC can reclaim it.

### 4.4 Working harness (verified — this exact pattern loaded all 180 files successfully)

```lua
local ROOT = ".../Questie/Localization/lookups/"
local EXPANSIONS = {"Classic","TBC","Wotlk","Cata","MoP"}
local LOCALES    = {"deDE","esES","esMX","frFR","koKR","ptBR","ruRU","zhCN","zhTW"}
local KINDS = {
  {dir="lookupQuests",  field="questLookup"},
  {dir="lookupNpcs",    field="npcNameLookup"},
  {dir="lookupObjects", field="objectLookup"},
  {dir="lookupItems",   field="itemLookup"},
}

local l10n, currentLocale
_G.GetLocale      = function() return currentLocale end
_G.QuestieLoader  = { ImportModule = function() return l10n end }

local function LoadLookup(expansion, kind, locale)
  local path = ROOT .. expansion .. "/" .. kind.dir .. "/" .. locale .. ".lua"
  l10n = { questLookup = {}, npcNameLookup = {}, objectLookup = {}, itemLookup = {} }  -- RESET
  currentLocale = locale                                                               -- RESET
  local chunk = assert(loadfile(path))
  chunk()
  local fn = l10n[kind.field][locale]
  assert(type(fn) == "function", path .. " did not register a lookup for " .. locale)
  return fn()   -- the table
end
```

The upstream reference implementations of this exact pattern:
* `Localization/lookups/<Exp>/lookupLoadstrings.test.lua` — `before_each` at ~line 116 builds
  a fresh `l10n = {itemLookup={}, npcNameLookup={}, objectLookup={}, questLookup={}}`, stubs
  `_G.QuestieLoader.ImportModule`, wraps `_G.loadstring` for error-line remapping, and each
  `describe("<locale>")` block sets `_G.GetLocale = function() return "<locale>" end`.
  `after_each` restores `_G.GetLocale`, `_G.QuestieLoader`, `_G.loadstring`.
* `cli/validate-localization.lua:20-22` sets `GetLocale = function() return "deDE" end` once
  and then `dofile`s the four Classic deDE files (lines 33-36) — single-locale only.

### 4.5 Optional: loadstring error-line remapping (worth copying)

Because the data lives inside a `loadstring` chunk, Lua reports syntax errors against the
in-memory chunk line, not the physical file line. `lookupLoadstrings.test.lua` (~line 125)
recovers the real line:
```lua
local chunkLine = tonumber(tostring(err):match(":(%d+):"))
local caller    = debug.getinfo(2, "Sl")
local fileLine  = caller.currentline + chunkLine - 1   -- caller.currentline == the loadstring([[ line
```

---

## 5. How Questie consumes this today

### 5.1 `l10n.lua` module surface — `Localization/l10n.lua:1-21`
```lua
---@class l10n
---@field itemLookup table<string, fun(): table>
---@field npcNameLookup table<string, fun(): table>
---@field objectNameLookup table<string, table<number, number>>
---@field objectLookup table<string, fun(): table>
---@field questLookup table<string, fun(): table>
---@field questLookupOverrides? fun(): table
local l10n = QuestieLoader:CreateModule("l10n")
...
l10n.itemLookup = {}
l10n.npcNameLookup = {}
l10n.objectNameLookup = {}
l10n.objectLookup = {}
l10n.questLookup = {}
```
(`l10n.itemLookupOverrides` is used at line 47 but is **missing from the `---@field` block** —
undocumented field.)

Active locale is module-local: `Localization/l10n.lua:30` `local locale = 'enUS'`, mutated only
by `l10n:SetUILocale(lang)` (line 205) via `l10n:GetFallbackLocale` (line 191) which falls back
to `'enUS'` for anything not in `supportedLocals`.

### 5.2 `l10n:Initialize()` — `Localization/l10n.lua:45-97` (verbatim)
```lua
function l10n:Initialize()
    local itemLookup = l10n.itemLookup[locale] and l10n.itemLookup[locale]() or {}
    for id, data in pairs(l10n.itemLookupOverrides and l10n.itemLookupOverrides() or {}) do
        itemLookup[id] = data
    end

    local questLookup = l10n.questLookup[locale] and l10n.questLookup[locale]() or {}
    for id, data in pairs(l10n.questLookupOverrides and l10n.questLookupOverrides() or {}) do
        questLookup[id] = data
    end

    local npcNameLookup = l10n.npcNameLookup[locale] and l10n.npcNameLookup[locale]() or {}
    local objectLookup = l10n.objectLookup[locale] and l10n.objectLookup[locale]() or {}

    -- Load item locales
    for id, name in pairs(itemLookup) do
        if QuestieDB.itemData[id] and name then
            QuestieDB.itemData[id][QuestieDB.itemKeys.name] = name
        end
    end

    -- data is {<questName>, {<questObjective>, ...}}
    -- Load quest locales
    for id, data in pairs(questLookup) do
        if QuestieDB.questData[id] then
            if data[1] then
                QuestieDB.questData[id][QuestieDB.questKeys.name] = data[1]
            end
            if data[2] then
                QuestieDB.questData[id][QuestieDB.questKeys.objectivesText] = data[2]
            end
        end
    end

    -- Load NPC locales
    for id, data in pairs(npcNameLookup) do
        if QuestieDB.npcData[id] and data then
            if type(data) == "string" then
                QuestieDB.npcData[id][QuestieDB.npcKeys.name] = data
            else
                QuestieDB.npcData[id][QuestieDB.npcKeys.name] = data[1]
                QuestieDB.npcData[id][QuestieDB.npcKeys.subName] = data[2]
            end
        end
    end

    -- Load object locales
    for id, name in pairs(objectLookup) do
        if QuestieDB.objectData[id] and name then
            QuestieDB.objectData[id][QuestieDB.objectKeys.name] = name
        end
    end
end
```

**Critical semantics for the overlay replacement:**
* Writes are **in-place mutations of `QuestieDB.questData` / `npcData` / `objectData` /
  `itemData`** (already-`loadstring`'d Lua tables) **before compile**.
* Quest: name and objectivesText each guarded by `if data[N] then` → a nil translation leaves
  the enUS base value. **Fallback-to-base is already the behaviour.**
* Npc: **NOT individually guarded.** `subName` is assigned unconditionally to `data[2]`, so a
  translated NPC with no localized subName **wipes the enUS subName to nil**. Reproducing
  current behaviour exactly means the npc subName overlay must clear, not fall back. (A
  fallback-to-base overlay is arguably a *fix*, but it is a behaviour change — flag it.)
* Npc branch tolerates a bare `string` value (`type(data) == "string"`). No file in the tree
  actually uses that form today — it is dead legacy tolerance.
* Only ids already present in the base DB are written (`if QuestieDB.<x>Data[id]`), so
  lookup ids with no base entity are dropped silently.
* Overrides (`lookupOverrides.lua`) are merged into `itemLookup`/`questLookup` **by whole row
  replacement** before the write loop. There are no npc/object overrides.

### 5.3 Boot order — `Modules/QuestieInit.lua`
```lua
-- QuestieInit.lua:111-135
local function loadFullDatabase()
    print("\124cFF4DDBFF [1/9] " .. l10n("Loading database") .. l10n("..."))
    QuestieInit:LoadBaseDB()
    print("\124cFF4DDBFF [2/9] " .. l10n("Applying database corrections") .. l10n("..."))
    coYield()
    QuestieCorrections:Initialize()
    print("\124cFF4DDBFF [3/9] " .. l10n("Initializing townsfolk") .. l10n("..."))
    coYield()
    Townsfolk.Initialize()
    print("\124cFF4DDBFF [4/9] " .. l10n("Initializing locale") .. l10n("..."))
    coYield()
    l10n:Initialize()                    -- <<< translations baked into questData here
    coYield()
    QuestieDB.private:DeleteGatheringNodes()
    print("\124cFF4DDBFF [5/9] " .. l10n("Optimizing waypoints") .. l10n("..."))
    coYield()
    QuestieCorrections:PreCompile()
end
```

### 5.4 `dbCompiledLang` — the recompile trigger to delete

Written, `Database/compiler.lua:1099-1110`:
```lua
    Questie.db.global.dbCompiledExpansion = WOW_PROJECT_ID

    if Questie.IsSoD then
        Questie.db.global.sod.dbCompiledOnVersion = QuestieLib:GetAddonVersionString()
        Questie.db.global.sod.dbCompiledLang = l10n:GetUILocale()
        Questie.db.global.sod.dbIsCompiled = true
        Questie.db.global.sod.dbCompiledCount = (Questie.db.global.sod.dbCompiledCount or 0) + 1
    else
        Questie.db.global.dbCompiledOnVersion = QuestieLib:GetAddonVersionString()
        Questie.db.global.dbCompiledLang = l10n:GetUILocale()
        Questie.db.global.dbIsCompiled = true
        Questie.db.global.dbCompiledCount = (Questie.db.global.dbCompiledCount or 0) + 1
    end
```

Read, `Modules/QuestieInit.lua:146-176`:
```lua
    l10n.InitializeUILocale()

    local dbIsCompiled, dbCompiledOnVersion, dbCompiledLang
    if Questie.IsSoD then
        dbIsCompiled = Questie.db.global.sod.dbIsCompiled or false
        dbCompiledOnVersion = Questie.db.global.sod.dbCompiledOnVersion
        dbCompiledLang = Questie.db.global.sod.dbCompiledLang
    else
        dbIsCompiled = Questie.db.global.dbIsCompiled or false
        dbCompiledOnVersion = Questie.db.global.dbCompiledOnVersion
        dbCompiledLang = Questie.db.global.dbCompiledLang
    end

    -- Check if the DB needs to be recompiled
    if (not dbIsCompiled) or (QuestieLib:GetAddonVersionString() ~= dbCompiledOnVersion) or (l10n:GetUILocale() ~= dbCompiledLang) or (Questie.db.global.dbCompiledExpansion ~= WOW_PROJECT_ID) then
        ...
        loadFullDatabase()
        QuestieDBCompiler:Compile()
        ...
    else
        l10n:Initialize()                -- <<< ALSO called on the cached path
        ...
        QuestieCorrections:MinimalInit()
    end
```
Note `l10n:Initialize()` is invoked on **both** branches (line 127 inside `loadFullDatabase`,
line 171 on the cached path).

`l10n.InitializeUILocale()` — `Localization/l10n.lua:99-114`:
```lua
function l10n.InitializeUILocale()
    if QUESTIE_LOCALES_OVERRIDE ~= nil then
        _InitializeLocaleOverride()
    end
    if Questie.db.global.questieLocaleDiff then
        l10n:SetUILocale(Questie.db.global.questieLocale);
    else
        if QUESTIE_LOCALES_OVERRIDE ~= nil then
            l10n:SetUILocale(QUESTIE_LOCALES_OVERRIDE.locale);
        else
            l10n:SetUILocale(GetLocale());
        end
    end
end
```
`QUESTIE_LOCALES_OVERRIDE` (`l10n.lua:118-133`) is a **third-party addon extension point**: an
external addon may inject its own `itemLookup/questLookup/npcNameLookup/objectLookup` functions
and a new locale code. Any replacement must keep an equivalent hook or explicitly drop it.

### 5.5 CLI harness reference — `cli/validate-localization.lua`
Loads Classic deDE, compiles, then asserts:
```lua
assert(QuestieDB.GetQuest(2).name == "Klaue von Scharfkralle")
assert(QuestieDB:GetNPC(3).name == "Fleischfresser")
assert(QuestieDB:GetObject(31).name == "Alte Löwenstatue")
assert(QuestieDB:GetItem(159).name == "Erfrischendes Quellwasser")
```
These are ready-made golden values for a QuestieDB parity test.

### 5.6 TOC load order
`Questie-Classic.toc:135-143` (others analogous at BCC:126-135, WOTLKC:134-143,
Cata:138-148, Mists:149-159):
```
Localization\l10n.lua
Localization\Translations\Translations.xml

Localization\lookups\lookupQuestCategories.lua
Localization\lookups\lookupZones.lua
Localization\lookups\Classic\lookupItems\lookupItems.xml
Localization\lookups\Classic\lookupNpcs\lookupNpcs.xml
Localization\lookups\Classic\lookupObjects\lookupObjects.xml
Localization\lookups\Classic\lookupQuests\lookupQuests.xml
```
**`lookupOverrides.lua` is NOT in `Questie-Classic.toc`** — it appears only in BCC (:131),
WOTLKC (:139), Cata (:142), Mists (:153). `lookupZonesCorrections.lua` only in Cata (:144)
and Mists (:155).

`Classic/lookupQuests/lookupQuests.xml` (all Classic/TBC/Wotlk/Cata manifests, 335 B):
```xml
<Ui xsi:schemaLocation="http://www.blizzard.com/wow/ui/ ..\FrameXML\UI.xsd">
  <Script file="deDE.lua"/>
  <Script file="esES.lua"/>
  ... (esMX, frFR, koKR, ptBR, ruRU, zhCN, zhTW)
</Ui>
```
MoP manifests (512 B) use the full modern xmlns header but the same 9 `<Script>` lines.

---

## 6. Separator / control-character safety audit

**Method**: loaded all 180 files, walked every *decoded* string (5 021 326 strings — item
names, object names, npc names+subNames, quest names, every objective line), byte-scanned for
`< 0x20` and `0x7F`, and substring-scanned for UTF-8 `U+2021` (`\226\128\161`) and `U+2020`
(`\226\128\160`).

### U+2021 DOUBLE DAGGER `‡` — **0 occurrences. Safe.**
### U+2020 DAGGER `†` — **0 occurrences.**
Also verified by raw `grep -ro '‡'` over the files: 0 hits.

### Control characters — 44 strings total, 50 characters. **These WILL bite.**

| byte | count (strings) | chars | where |
|---|---|---|---|
| `0x05` | 39 | 39 | item names, MoP (all 9 locales) + TBC frFR/ruRU |
| `0x0A` (LF) | 3 | 9 | `Classic/lookupQuests/esMX.lua` objective lines |
| `0x7F` (DEL) | 2 | 2 | `Classic/lookupNpcs/zhTW.lua` + `TBC/lookupNpcs/zhTW.lua`, npc 24996 name |

Full list (`<XX>` = hex of the offending byte):

```
0x7F  Classic/lookupNpcs/zhTW.lua:[24996].name        <7F>海員刃語        (file line 16109)
0x7F  TBC/lookupNpcs/zhTW.lua:[24996].name            <7F>海員刃語        (file line 16109)

0x0A  Classic/lookupQuests/esMX.lua:[462].obj[1]      Kill Maruk Wyrmscale<0A><0A>Report to Valstag Ironjaw in Menethil Harbor.        (file line 456)
0x0A  Classic/lookupQuests/esMX.lua:[5053].obj[1]     Test quest to see if mail is sent to your mailbox. Just hit complete, then go check your mail.<0A><0A>Have a nice day.   (file line 2371)
0x0A  Classic/lookupQuests/esMX.lua:[5101].obj[1]     Kill 6 sheep for Benny and then run like hell!<0A><0A><0A><0A><0A>Just kidding... about the running part.   (file line 2405)

0x05  MoP/lookupItems/{deDE,esES,esMX,koKR,ptBR,ruRU,zhCN,zhTW}.lua:[22036]   QAEnchant Bracer +4 Mana<05>
0x05  MoP/lookupItems/frFR.lua:[22036]                                        QAEnchant Bracer +4 Mana<05>
0x05  MoP/lookupItems/{deDE,esES,esMX,koKR,ptBR,ruRU,zhCN,zhTW}.lua:[29870]   QAEnchant Chest +6 Mana<05>
0x05  MoP/lookupItems/frFR.lua:[29870]                                        [QA]Enchantement Plastron +6 Mana<05>
0x05  MoP/lookupItems/{deDE,esES,esMX,koKR,ptBR,zhCN,zhTW}.lua:[30199]        QAEnchant Boots +4 Health & Mana<05>
0x05  MoP/lookupItems/ruRU.lua:[30199]                                        QAEnchant Boots +4 Health & Mana<05>
0x05  MoP/lookupItems/{deDE,esES,esMX,koKR,ptBR,zhCN,zhTW}.lua:[30877]        QAEnchant Bracer +6 Mana<05> Sec
0x05  MoP/lookupItems/frFR.lua:[30877]                                        [QA]Enchantement Brassards +6 Mana<05> Sec
0x05  MoP/lookupItems/ruRU.lua:[30877]                                        Тест: зачаровывание наручей, мана +6<05> сек.
0x05  TBC/lookupItems/frFR.lua:[29870]                                        AQEnchantement Plastron +6 Mana<05>
0x05  TBC/lookupItems/frFR.lua:[30877]                                        AQEnchantement Brassards +6 Mana<05> Sec
0x05  TBC/lookupItems/ruRU.lua:[30199]                                        Тест: зачаровывание обуви, здоровье и мана +4<05>
0x05  TBC/lookupItems/ruRU.lua:[30877]                                        Тест: зачаровывание наручей, мана +6<05> сек.
```
(All are junk QA/test entities — ids 22036, 29870, 30199, 30877, 24996, 462, 5053, 5101.
Sanitising or dropping them is defensible; silently corrupting them is not.)

### Escape sequences in the raw source (what to expect when re-emitting)

The payload is `[[…]]` at the outer level (backslash literal) but the *inner* text is Lua
source with `"…"` strings, so backslashes there **are** escapes. Raw two-char sequences
present across all data files:
```
\"   24838   escaped double-quote inside a name / objective line
\\      35   literal backslash (decodes to \)   e.g. Cata/lookupItems/frFR.lua:16841
\5      39   decodes to byte 0x05               e.g. TBC/lookupItems/frFR.lua:30877
\n      19   decodes to newline 0x0A            only Classic/lookupQuests/esMX.lua:{456,2371,2405}
```
No `\r`, `\t`, `\ddd` other than `\5`. A generator that re-emits Lua source must escape at
minimum `"`, `\`, and any byte `<0x20`/`0x7F`.

### Pipe `|` characters
Present in raw source of some quest files (e.g. `Wotlk/lookupQuests/esMX.lua` 256 lines,
`Cata/lookupQuests/esMX.lua` 211). These are WoW escape/UI sequences (`|c`, `|r`, `|4`) or
literal pipes in text. `|` is **not** a safe separator for this data.

---

## Data-quality gotchas found while loading (stale / mismatched files)

Row counts are the authoritative signal. These files are clearly copies of the wrong
expansion or truncated dumps:

| File | rows | Should look like | Evidence |
|---|---|---|---|
| `Classic/lookupNpcs/zhTW.lua` | 16 731, ids 3..29095 | other Classic npc files: 9 188, ids 3..19283 | it is a copy of **TBC** zhTW (identical 16 731) |
| `Classic/lookupObjects/esMX.lua` | 5 867 | other Classic object files ~1 100-1 200 | TBC/Wotlk-era object data |
| `Classic/lookupObjects/zhTW.lua` | 12 164, max id 500001 | ~1 200, max id 400001 | ditto |
| `TBC/lookupNpcs/esMX.lua` | 9 188, ids 3..19283 | 17 390 | **Classic** data |
| `TBC/lookupItems/esMX.lua` | **536**, ids 25..1467 (18 884 B) | ~26 000 | severely truncated stub |
| `TBC/lookupNpcs/zhTW.lua` | 16 731 | 17 390 | slightly short (same file as Classic zhTW) |
| `MoP/lookupQuests/esMX.lua` | 12 256 | ~15 300 | header says `-- ! THIS IS A RAW COPY OF THE CATA DATA` |
| `MoP/lookupQuests/zhTW.lua` | 12 536 | ~15 300 | same header banner |
| `Cata/lookupObjects/ptBR.lua` | 3 396 (122 717 B) | ~19 600 | partial |
| `MoP/lookupObjects/ptBR.lua` | 4 307 (153 553 B) | ~20 500 | partial |
| `Wotlk/lookupObjects/zhCN.lua` | 6 067, max id 420002 | ~2 100, max 400001 | mismatched |
| `Wotlk/lookupItems/zhCN.lua` | 37 252, max id 279578 | 36 749, max 190309 | mismatched |
| `Wotlk/lookupQuests/zhCN.lua` | 9 365, max id 98183 | 9 302, max 78753 | mismatched |
| `Wotlk/lookupNpcs/zhCN.lua` | 26 456, max id 262258 | 26 450, max 42078 | mismatched |
| `MoP/lookupQuests/zhCN.lua` | 15 791, max id 91786 | max 82985 | mismatched |

None of these break loading; they are just wrong/incomplete data. **The generator must not
assume locale row-count parity within an expansion.** Reference row counts per expansion
(deDE, representative):

| Expansion | quests | npcs | objects | items |
|---|---|---|---|---|
| Classic | 4 244  |  9 188 |  1 212 | 22 670 |
| TBC     | 6 540  | 17 390 |  2 150 | 26 330 |
| Wotlk   | 9 302  | 26 450 |  2 150 | 36 749 |
| Cata    | 12 743 | 42 976 | 19 836 | 65 254 |
| MoP     | 15 632 | 53 597 | 20 756 | 88 183 |

Also: 115 Classic quests have a *different objective-line count* between locales
(e.g. quest 9142: deDE has 0 lines, koKR has 1). Quest-objective arrays are per-locale, not a
per-line parallel array. Any per-line encoding must tolerate a differing line count per locale.

---

## Prototype divergences (informational — prototypes are stale, Questie is truth)

* **Locale order differs between prototypes and QuestieDB config.** `Getters/src/get/l10n.lua:39`
  uses `{ "ptBR", "ruRU", "deDE", "koKR", "esES", "esMX", "frFR", "zhCN", "zhTW" }`, while
  `QuestieDB/src/config.lua:79` uses `{ "deDE", "esES", "esMX", "frFR", "koKR", "ptBR",
  "ruRU", "zhCN", "zhTW" }`. Order is load-bearing (the decoder captures the Nth segment).
  Do not copy the Getters order.
* `toc-database/src/config.lua:19-48` field contract: Item `{name}`, Npc `{name, subName}`,
  Object `{name}` (`scalar = true`), Quest `{title(1), objectives(2)}` with
  `description/details/requestItems/offerReward/endText` commented out. Matches Questie's four
  fields; `title` is Questie's `name`, `objectives` is Questie's `objectivesText`.
* `Getters/data/l10n/Era/l10nQuestData.lua-table` rows are `{{ nameJoined, nil, objectivesJoined }}`
  — i.e. the prototype reserved index 2 and put objectives at index 3, and it collapsed the
  **objective line array into a single string per locale**. Questie's real data has up to
  15 lines with meaningful empties. Verify how QuestieDB intends to encode the list dimension
  before assuming a flat locale-join is sufficient.
* `QuestieDB/docs/storage-format.md:98` and `QuestieDB/src/config.lua:80` both fix the
  separator at `‡` U+2021 — confirmed collision-free against the real data (0 occurrences).

---

## Quick reference: everything the generator needs

```
LOCALES  = { "deDE","esES","esMX","frFR","koKR","ptBR","ruRU","zhCN","zhTW" }   -- no enUS
EXPANSIONS = { "Classic","TBC","Wotlk","Cata","MoP" }
PATH     = Localization/lookups/<Expansion>/<lookupDir>/<locale>.lua

lookupQuests  -> l10n.questLookup[loc]    -> function -> { [id] = {name?, {objLine,...}?} }  -> Quest.name(1), Quest.objectivesText(8)
lookupNpcs    -> l10n.npcNameLookup[loc]  -> function -> { [id] = {name?, subName?} }        -> Npc.name(1), Npc.subName(14)
lookupObjects -> l10n.objectLookup[loc]   -> function -> { [id] = name }                     -> Object.name(1)
lookupItems   -> l10n.itemLookup[loc]     -> function -> { [id] = name }                     -> Item.name(1)

Stub: _G.GetLocale (mutable), _G.QuestieLoader.ImportModule -> fresh l10n table per file.
Reset: new l10n table + currentLocale before every loadfile.
Assert: type(l10n[field][locale]) == "function" after each load.
```
