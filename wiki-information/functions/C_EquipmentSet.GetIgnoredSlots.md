## Title: C_EquipmentSet.GetIgnoredSlots

**Content:**
Returns ignored slots of an equipment set.
`slotIgnored = C_EquipmentSet.GetIgnoredSlots(equipmentSetID)`

**Parameters:**
- `equipmentSetID`
  - *number*

**Returns:**
- `slotIgnored`
  - *boolean* - indexed by InventorySlotId