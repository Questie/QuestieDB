## Title: IsTrackedAchievement

**Content:**
Returns if an achievement is currently being tracked.
`tracked = GetAchievementNumCriteria(achievementID)`

**Parameters:**
- `achievementID`
  - Uniquely identifies each achievement

**Returns:**
- `eligible`
  - *boolean*