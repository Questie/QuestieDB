## Title: CreateWindow

**Content:**
Needs summary.
`window = CreateWindow()`

**Parameters:**
- `popupStyle`
  - *boolean?* = true

**Returns:**
- `window`
  - *SimpleWindow?*

**Description:**
This function is disabled in public clients and will always return nil.