## Title: QuestChooseRewardError

**Content:**
Throws an error when the quest reward method doesn't work.
`QuestChooseRewardError()`

**Description:**
Fires a `UI_ERROR_MESSAGE ERR_QUEST_MUST_CHOOSE` error.