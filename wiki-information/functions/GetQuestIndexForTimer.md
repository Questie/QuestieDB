## Title: GetQuestIndexForTimer

**Content:**
Gets the quest log index of a quest being timed.
`questIndex = GetQuestIndexForTimer(timerId)`

**Parameters:**
- `timerId`
  - *number* - The ID of a quest timer.

**Returns:**
- `questIndex`
  - *number* - The quest log's index of the timed quest.