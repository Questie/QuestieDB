## Title: UnitIsPossessed

**Content:**
Returns true if the unit is currently under control of another (e.g. Mind Control).
`isTrue = UnitIsPossessed(unit)`

**Parameters:**
- `unit`
  - *string* - UnitId

**Returns:**
- `isTrue`
  - *boolean* - true if the unit is possessed, false otherwise.