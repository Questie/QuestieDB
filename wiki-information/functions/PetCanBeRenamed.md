## Title: PetCanBeRenamed

**Content:**
Returns true if the pet can be renamed.
`canRename = PetCanBeRenamed()`

**Returns:**
- `canRename`
  - *boolean* - true if the player's pet can be renamed.

**Usage:**
```lua
if (PetCanBeRenamed()) then
  PetRename("Fuzzy Wuzzy");
end
```