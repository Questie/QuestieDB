## Title: C_UI.ShouldUIParentAvoidNotch

**Content:**
UIParent will shift down to avoid notch if true. This does not mean there is a notch.
`willAvoidNotch = C_UI.ShouldUIParentAvoidNotch()`

**Returns:**
- `willAvoidNotch`
  - *boolean*