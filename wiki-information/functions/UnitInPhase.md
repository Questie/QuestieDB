## Title: UnitInPhase

**Content:**
Returns if a unit is in the same phase.
`inPhase = UnitInPhase(unit)`

**Parameters:**
- `unit`
  - *string* : UnitId

**Returns:**
- `inPhase`
  - *boolean*

**Reference:**
- `UNIT_PHASE`