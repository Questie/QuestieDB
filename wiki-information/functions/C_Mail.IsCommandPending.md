## Title: C_Mail.IsCommandPending

**Content:**
Returns true if the current mail command is still processing.
`isCommandPending = C_Mail.IsCommandPending()`

**Returns:**
- `isCommandPending`
  - *boolean*