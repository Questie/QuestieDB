## Title: C_SuperTrack.GetSuperTrackedQuestID

**Content:**
Returns the quest ID currently being tracked, if set. Replaces `GetSuperTrackedQuestID`.
`questID = C_SuperTrack.GetSuperTrackedQuestID()`

**Returns:**
- `questID`
  - *number?*