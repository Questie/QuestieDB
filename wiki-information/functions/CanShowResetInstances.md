## Title: CanShowResetInstances

**Content:**
Returns true if the character can currently reset their instances.
`canReset = CanShowResetInstances()`

**Returns:**
- `canReset`
  - *boolean* - true if player can reset instances