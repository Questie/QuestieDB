## Title: KBSetup_GetCategoryCount

**Content:**
Returns the number of categories.
`count = KBSetup_GetCategoryCount()`

**Parameters:**
- `()`

**Returns:**
- `count`
  - *number* - Number of categories.

**Description:**
This is only available when `KBSetup_IsLoaded()` is true.